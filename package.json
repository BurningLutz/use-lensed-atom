{
  "name": "use-lensed-atom",
  "version": "3.0.0-alpha",
  "description": "A React Hook that supports lensed atom.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepare": "npm run build",
    "postversion": "git push && git push --tags",
    "test": "ts-mocha -p tsconfig.test.json test/**/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BurningLutz/use-lensed-atom.git"
  },
  "keywords": [
    "lens",
    "atom",
    "hook"
  ],
  "author": "BurningLutz",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/BurningLutz/use-lensed-atom/issues"
  },
  "homepage": "https://github.com/BurningLutz/use-lensed-atom#readme",
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "@types/react": "^18.0.15",
    "fast-check": "^3.0.1",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.7.4"
  }
}
